<% if !user_signed_in? %>
    <nav class="navbar navbar-expand-lg navbar bg-light">
        <div class="container proto-container" style="padding-left: 0px;">
            <a class="navbar-brand" href=<%= root_url %>>
              <%=  image_tag("proto-logo.png", style: "margin-top: 7px;", class: "d-inline-block align-top") %>
            </a>
        </div>
    </nav>
<% else %>
    <% if @on_an_account_page %>
      <nav class="navbar navbar-expand-lg navbar bg-light">
        <div class="bootstrap-container container" style="padding-left: 0px;">
          <a class="navbar-brand" href=<%= account_path(@account) %>>
            <%=  image_tag("proto-logo.png", style: "margin-top: 7px;", class: "d-inline-block align-top") %>
          </a>
            <ul class="navbar-nav mr-auto" style="margin-left: 0px !important; margin-right: auto;">
              <% if !is_admins_controller or (controller_name != 'accounts' and ['new','create'].include?(action_name)) %>
                <%= render partial: "layouts/dropdown_teams" %>
              <% end %>
              <% if @folder.present? and @folder.id.present? %>
                <li class="nav-item">
                  <a class="nav-link">/</a>
                </li>
                <li class="nav-item">
                  <%= link_to @folder.name, account_folder_path(@account, @folder), class: "nav-link" %>
                </li>
                <% if controller_name == "streams" %>
                  <li class="nav-item"><a class="nav-link">/</a></li>
                  <li class="nav-item"><%= link_to "Streams", account_folder_streams_path(@account, @folder), class: "nav-link"%></li>
                <% elsif controller_name == "view_casts" %>
                  <li class="nav-item"><a class="nav-link">/</a></li>
                  <li class="nav-item"><%= link_to "Cards", account_folder_view_casts_path(@account, @folder), class: "nav-link"%></li>
                <% end %>
              <% end %>
            </ul>
            <ul class="navbar-nav ml-auto">
                  <% if @folder.present? and @folder.id.present? and !@folder.is_trash %>
                      <% if controller_name == "view_casts" and action_name != "new" %>
                        <li class="nav-item"><%= link_to '+ Add a card', new_account_folder_view_cast_path(@account, @folder), data: {turbolinks: false}, class: "nav-link btn btn-sm btn-primary", style: "margin-top: 5px;" %></li>
                      <% elsif controller_name == "streams" and action_name != "new"  %>
                        <li class="nav-item"><%= link_to '+ Add a stream', new_account_folder_stream_path(@account, @folder), data: {turbolinks: false}, class: "nav-link btn btn-sm btn-primary", style: "margin-top: 5px;" %></li>
                      <% end %>

                  <% elsif controller_name == "audios" and action_name != "new"  %>
                      <li class="nav-item"><%= link_to '+ Upload an audio', "#", class: "nav-link btn btn-sm btn-primary",style: "margin-top: 5px;", "data-toggle" => "modal", "data-target" => "#fileUploadModal" %></li>
                      <%= render partial: "audios/form" %>

                  <% end %>
                  
                  <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                      Site: <%= @site.domain %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                      <% if controller_name == "folders" and @folder.id.present? and current_user.is_admin_from_pykih %>
                        <%= link_to "Add multiple cards", account_folder_uploads_path(@account, @folder), "data-turbolinks"=> false, class: "dropdown-item" %>
                      <% end %>
                      <%= link_to "Settings", edit_account_site_path(@account, @site), "data-turbolinks"=> false, class: "dropdown-item"  %>
                      <%= link_to 'People', nil, class: "dropdown-item" %>
                      <%= link_to 'Categories', account_site_ref_categories_path(@account, @site), class: "dropdown-item" %>
                    </div>
                  </li>
                  
                  
                  <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                      <%= image_tag avatar_url(current_user.email), class: "rounded", style: "width: 20px;" %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                      <h6 class="dropdown-header" style="font-size: 11px;">Account: <%= @account.username %></h6>
                      <%= link_to "Settings", edit_account_path(@account), "data-turbolinks"=> false, class: "dropdown-item" %>
                      <%= link_to "Image Bank", account_images_path(@account), "data-turbolinks"=> false, class: "dropdown-item"  %>
                      <% if 1 == 2 %>
                          <%= link_to "Audio Bank", account_audios_path(@account), "data-turbolinks"=> false, class: "dropdown-item"  %>
                      <% end %>
                      <div class="dropdown-divider"></div>
                      <h6 class="dropdown-header" style="font-size: 11px;">Me</h6>
                      <% if current_user.is_admin_from_pykih %>
                        <%= link_to 'Online users', admin_online_users_path, class: "dropdown-item", style: "color: gold;" %>
                      <% end %>
                      <% if current_user.can_publish_link_sources %>
                          <%= link_to "Ref: Publishers", ref_link_sources_path, class: "dropdown-item", style: "color: gold;" %>
                      <% end %>
                      <%= link_to 'Profile', edit_user_path(current_user), class: "dropdown-item" %>
                      <%= link_to 'Settings', edit_user_registration_path, class: "dropdown-item" %>
                      <%= link_to "Sign out", destroy_user_session_path, class: "dropdown-item" %>
                    </div>
                  </li>
                </ul>
          </div>
        </div>
      </nav>
    <% else %>
      <nav class="navbar navbar-expand-lg navbar bg-light">
        <div class="container" style="padding-left: 0px;">
          <a class="navbar-brand" href=<%= root_url %>>
            <%=  image_tag("proto-logo.png", style: "margin-top: 7px;", class: "d-inline-block align-top") %>
          </a>
            <ul class="navbar-nav mr-auto" style="margin-left: 0px !important; margin-right: auto;">
              <%  if !is_admins_controller and (controller_name != 'accounts' and ['new','create'].include?(action_name))  %>
                <%= render partial: "layouts/dropdown_teams" %>
              <% end %>
            </ul>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <%= image_tag avatar_url(current_user.email), class: "rounded", style: "width: 20px;" %>
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                  <% if current_user.is_admin_from_pykih %>
                    <%= link_to 'Online users', admin_online_users_path, class: "dropdown-item", class: "dropdown-item", style: "color: gold;" %>
                  <% end %>
                  <% if current_user.can_publish_link_sources %>
                      <%= link_to "Ref: Publishers", ref_link_sources_path, class: "dropdown-item", class: "dropdown-item", style: "color: gold;" %>
                  <% end %>
                  <%= link_to 'Profile', edit_user_path(current_user), class: "dropdown-item" %>
                  <%= link_to 'Settings', edit_user_registration_path, class: "dropdown-item" %>
                  <%= link_to "Sign out", destroy_user_session_path, class: "dropdown-item" %>
                </div>
              </li>
            </ul>
        </div>
      </nav>
    <% end %>
<% end %>
