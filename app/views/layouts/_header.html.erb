<% if user_signed_in? %>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid" style="padding-left: 0px;">
            <a class="navbar-brand" href=<%= @on_an_account_page ? account_site_path(@account, @site) : root_url %>>
              <%=  image_tag("proto-logo.png", style: "margin-top: 4px; height: 23px", class: "d-inline-block align-top") %>
            </a>
            <ul class="navbar-nav" style="margin-left: 0px !important; margin-right: auto;">
                <%= render partial: "layouts/dropdown_teams" %>
                <%= render partial: "layouts/dropdown_breadcrums" %>
            </ul>
            <%= render partial: "layouts/dropdown_personal" %>
        </div>
    </nav>
<% end %>