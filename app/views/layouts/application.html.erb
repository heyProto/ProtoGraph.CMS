<!DOCTYPE html>
<html lang="en">
<head>
  <% if @site.present? %>
      <title><%= @site.name %> | Proto Platform</title>
  <% else %>
      <title>Proto Platform</title>
  <% end %>
  <%= csrf_meta_tags %>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="turbolinks-visit-control" content="reload">
  <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Bitter:400,700|Cabin:400,400i,500,700" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body data-turbolinks=<%= (controller_name == "view_casts" and ['new', 'edit'].include?(action_name)) ? "false" : "true" %>>
        <div class="container-fluid" style="padding: 0px;">
            <div class="row" style="margin:0px; z-index: 999; position: relative;">
                <div class="proto-app-navbar-masthead">
                    <h1>
                        <% if @site.present? %>
                            <%= link_to image_tag("proto-dark.png", style: "height: 24px;"), account_site_path(@account, @site) %>
                        <% else %>
                            <%= link_to image_tag("proto-dark.png", style: "height: 24px;"), root_url %>
                        <% end %>
                        <% if user_signed_in? %>
                            <span style="float: right; position: relative;">
                                
                                <div class="dropdown">
                                  <a class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"  style="color: gray !important; font-size: 11px;">
                                    <%= image_tag avatar_url(current_user.email), class: "rounded", style: "width: 24px;" %>
                                  </a>
                                  <div class="dropdown-menu">
                                    <%= link_to 'Profile', edit_user_path(current_user), class: "dropdown-item" %>
                                    <%= link_to 'Settings', edit_user_registration_path, class: "dropdown-item" %>
                                    <%= link_to "Sign out", destroy_user_session_path, class: "dropdown-item" %>
                                    <% if current_user.is_admin_from_pykih %>
                                        <%= link_to 'Ref: Link Sources', ref_link_sources_path, class: "dropdown-item", style: "font-size: 10px;" %>
                                        <%= link_to "Admin: User Sessions", admin_user_sessions_path, class: "dropdown-item", style: "font-size: 10px;" %>
                                    <% end %>
                                  </div>
                                </div>
                            </span>
                        <% end %>
                    </h1>
                </div>
                <% if @site.present? %>
                    <div class="proto-app-navbar-proto-container">
                        <div class="proto-app-navbar">
                            <div id="proto_app_header">
                                <div class="proto-app-navbar">
                                    <div class="proto-app-navbar-logo-holder">
                                        <div class="proto-app-navbar-logo" style="display: inline-block;">
                                            <a href="<%= @site.present? ? account_site_path(@account, @site) : root_url %>">
                                                <img src="https://cdn.protograph.pykih.com/Assets/proto-app/img/proto-color-logo.png" class="colored-logo">
                                                <img src="https://cdn.protograph.pykih.com/Assets/proto-app/img/proto-grey-logo.png" class="grey-logo">
                                            </a>
                                        </div>
                                        <h1 class="proto-app-navbar-project-name" id="homepage_nav">
                                            <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= truncate @site.domain, length: 40, ommission: ".." %></a>
                                            <% if @accounts.count > 1 %>
                                                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                                    <% @accounts.each do |a| %>
                                                        <%= link_to "#{a.username}: #{a.site.domain}", account_site_path(a, a.site), class: "dropdown-item", style: "text-align: left; font-size: 12px !important; padding: 0px 20px !important; color: black !important;" %>
                                                    <% end %>
                                                </div>
                                            <% end %>
                                        </h1>
                                        <div class="proto-app-navbar-project-by" style="display: inline-block;">
                                            <%= link_to "Adminland", edit_account_site_path(@account, @site)%>
                                        </div>
                                    </div>
                                    <div class="proto-app-navbar-navigation-bar">
                                        <div class="proto-app-navbar-page-navigation">                    
                                            <div class="proto-app-navbar-navigation-scroll">
                                                <div class="proto-app-navbar-page-links">
                                                    <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Landings</a>
                                                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink" style="z-index: 10000000 !important;">
                                                        <% @all_verticals.each do |ref_category| %>            
                                                            <% d = ref_category.vertical_page %>
                                                            <li>
                                                                <% if d.present? %>
                                                                    <%= link_to "# " + ref_category.name, edit_account_site_page_path(@account, @site, d, folder_id: @folder.present? ? @folder.id : nil), "data-turbolinks"=> false, class: "dropdown-item" %>
                                                                <% else %>
                                                                    <%= "# " + ref_category.name %>
                                                                <% end %>
                                                            </li>
                                                        <% end %>
                                                        <li><%= link_to "Add Section", sections_account_site_path(@account, @site), "data-turbolinks"=> false, class: "dropdown-item" %></li>
                                                    </div>
                                                </div>
                                                <div class="proto-app-navbar-page-links">
                                                    <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Assets</a>
                                                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink" style="z-index: 10000000 !important;">
                                                        <li><%= link_to "Images", account_images_path(@account), "data-turbolinks"=> false, class: "dropdown-item" %></li>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <% end %>
            </div>
            <div class="container" style="width: 1260px; margin-left: auto; margin-right: auto; position: relative; padding-top: 0px">
                <%= yield %>
            </div>
        </div>
    
    

    
  <% config = {} %>
  <% if notice.present? %>
    <% config = {text: notice, notify: "success"} %>
  <% end %>
  <% if alert.present? %>
    <% config = {text: alert, notify: "error"} %>
  <% end %>

  <script type="text/javascript">
    var notice = <%= config.to_json.html_safe %>;
    if (notice.notify) generate_notify(notice);
  </script>
</body>
</html>