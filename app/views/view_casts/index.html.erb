<div class="ui stackable sixteen column grid">
    <% @view_casts.each do |d| %>
        <div class="seven wide column">
            <h3></h3>
            <% ss %>
            <iframe src="https://www.youtube.com/embed/JBRI19x-1os" frameborder="0" allowfullscreen></iframe>
        </div>
    <% end %>
</div>
