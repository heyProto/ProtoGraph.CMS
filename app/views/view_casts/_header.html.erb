<h1>
    <%= folder_name_(@folder)%>
    <% if !@folder.is_trash %>
        <small style="font-size: 14px;"><%= link_to "Edit this workspace", edit_site_folder_path(@site, @folder) %></small>
    <% end %>
    <% if !@folder.is_trash %>
        <span style="float: right;"><%= link_to '+ Add a card', new_site_folder_view_cast_path(@site, @folder), data: {turbolinks: false}, class: "btn btn-dark btn-sm" %></span>
    <% end %>
</h1>
<br/>