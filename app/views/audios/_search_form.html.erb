<%= search_form_for @q, url: account_audios_path(@account), class: "form-group form-inline" do |f| %>
  <div class="form-group">
    <%= f.label :search_audios, class: "sr-only control-label" %>
    <%= f.search_field :tags_name_or_name_or_description_cont, class: "form-control form-control-sm" %>
  </div>
  <div class="form-group">
    <%= f.submit :search, class: "btn btn-outline" %>
  </div>
<% end %>
