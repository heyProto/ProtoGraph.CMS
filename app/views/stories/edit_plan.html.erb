<%= content_for :page_title do %>
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
          <% if @folder.present? %>
              <li class="breadcrumb-item"><%= link_to truncate(folder_name_trunc_(@folder), length: 24, ommission: ".."), account_site_folder_view_casts_path(@account, @site, @folder), "data-turbolinks"=> false %></li>
          <% end %>
          <li class="breadcrumb-item"><%= link_to "Stories", account_site_stories_path(@account, @site, folder_id: @folder.id), "data-turbolinks"=> false %></li>
          <li class="breadcrumb-item active" aria-current="page"><%= @page.one_line_concept %></li>
      </ol>
    </nav>
<% end %>
<%= render partial: "stories/stories_header" %>

<div class="row">
    <div class="col-sm-13">
        <div style="margin-left: auto; margin-right: auto; width: 80%;">
            <% if @page.intersection.present? %>
                <div class="story-pitch-intersection">
                    <%= @page.intersection.name %>
                    <% if @page.sub_intersection.present? %>
                        <div class="story-pitch-sub-intersection">
                            <%= @page.sub_intersection.name %>
                        </div>
                    <% end %>
                </div>
            <% end %>
            <h2 style="margin-top: 10px; font-size: 1.3em;"><%= best_in_place @page, :one_line_concept, url: account_site_page_path(@account, @site, @page) %></h2>
            <%= best_in_place @page, :description, as: :textarea, url: account_site_page_path(@account, @site, @page), place_holder: "Edit the description" %>
            <br/>
            <br/>
            <br/>
            <div class="row">
                <div class="col-sm-6">
                    <h4 style="color: gray; font-weight: 100;">ELEMENTS</h4>
                    <%= best_in_place @page, :has_image_other_than_cover, as: :checkbox, collection: {false: "No", true: "Yes"}, url: account_site_page_path(@account, @site, @page) %>
                    <%= image_tag "glyphicons-139-picture.png", height: "16px", class: @page.has_image_other_than_cover ? nil : "disabled" %>
                    &nbsp;&nbsp;
                    <%= best_in_place @page, :has_audio, as: :checkbox, collection: {false: "No", true: "Yes"}, url: account_site_page_path(@account, @site, @page) %>
                    <%= image_tag "glyphicons-301-microphone.png", height: "16px", class: @page.has_audio ? nil : "disabled" %>
                    &nbsp;&nbsp;
                    <%= best_in_place @page, :has_video, as: :checkbox, collection: {false: "No", true: "Yes"}, url: account_site_page_path(@account, @site, @page) %>
                    <%= image_tag "glyphicons-181-facetime-video.png", height: "13px", class: @page.has_video ? nil : "disabled" %>
                    &nbsp;&nbsp;
                    <%= best_in_place @page, :has_data, as: :checkbox, collection: {false: "No", true: "Yes"}, url: account_site_page_path(@account, @site, @page) %>
                    <%= image_tag "glyphicons-43-pie-chart.png", height: "16px", class:  @page.has_data ? nil : "disabled" %>
                </div>
                <div class="col-sm-4">
                    <h4 style="color: gray; font-weight: 100;">DUE DATE</h4>
                    <%= best_in_place @page, :due, as: :date, url: account_site_page_path(@account, @site, @page) %>
                </div>
                <div class="col-sm-6">
                    <h4 style="color: gray; font-weight: 100;">PUBLISH DATE</h4>
                    <%= best_in_place @page, :published_at, as: :date, url: account_site_page_path(@account, @site, @page) %>
                </div>
            </div>
            <br/>
            <br/>
            <%= render partial: "page_todos/index"%>
        </div>
    </div>
	<div class="col-sm-3 help-text">
        <%= render partial: "stories/stories_sidebar" %>
		<!--<h2>Email Settings</h2>
		<p>Your default support email address is prototype@pro.to. Any email sent here gets automatically converted into a ticket that you can get working on.</p>
		<p>You can configure your Freshdesk account to use a support email in your own domain, like <a href="">support@mycompany.com</a> by forwarding emails from this address to <a href="">protoprototype@heyproto.freshdesk.com</a>. To create a new support email box, click “Edit” under global email settings.</p>-->
	</div>
</div>